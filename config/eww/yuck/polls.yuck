; Date / Time Polls

(defpoll date :interval "10m" "date '+%A, %d %b'")
(defpoll time
  :interval "10s"
  :initial "initial-value" 
  `date +%H:%M' '%P`
)

; System Stats

(defpoll memory-percent :initial 0 :interval "2m" "./scripts/system memory_percent_used")
(defpoll disk-percent :initial 0 :interval "5m" "./scripts/system storage_percent_used")

(defpoll cpu-temp :initial 0 :interval "1m" "./scripts/system cpu_temp")
(defpoll gpu-temp :initial 0 :interval "1m" "./scripts/system gpu_temp")

(defpoll gpu-psu-draw :initial 0 :interval "1m" "./scripts/system gpu_psu_draw")

; Workspaces Listener

(deflisten workspace "./scripts/currentWorkspace")